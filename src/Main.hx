import cocos.Cocos;
import cocos.display.Node;

class CustomNode extends Node {
    public function new() {
        super();
    }
}

class Main {
    static function main() {
        var node = new Node();
        var defaultFps = Cocos.DEFAULT_FPS;
    }
}